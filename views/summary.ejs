<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= summary.game_title %> - Rules Summary</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/print.css" media="print">
</head>
<body>
  <header class="summary-header no-print">
    <div class="container header-row">
      <a href="/" class="back-link">&larr; Summarize another game</a>
      <div class="header-actions">
        <a href="/summary/<%= summary.id %>/raw" class="btn btn-secondary">Raw Markdown</a>
        <button onclick="window.print()" class="btn btn-primary">Print / Save as PDF</button>
      </div>
    </div>
  </header>

  <main class="container summary-content">
    <%- html %>
  </main>

  <footer class="no-print">
    <div class="container">
      <p class="meta">
        Generated from <strong><%= summary.original_filename %></strong>
        on <%= new Date(summary.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
      </p>
    </div>
  </footer>
</body>
</html>
